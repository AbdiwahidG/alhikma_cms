import 'package:flutter/material.dart';

class WeeklyWagesScreen extends StatelessWidget {
  const WeeklyWagesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Weekly Wages Screen'),
      ),
    );
  }
} 